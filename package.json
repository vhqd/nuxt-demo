{
  "name": "blog-nuxt",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development SERVER_ENV=development nuxt",
    "dev:production": "cross-env NODE_ENV=development SERVER_ENV=production nuxt",
    "build": "cross-env NODE_ENV=production SERVER_ENV=development nuxt build",
    "build:production": "cross-env NODE_ENV=production SERVER_ENV=production nuxt build",
    "start": "cross-env NODE_ENV=production SERVER_ENV=development nuxt start",
    "start:production": "cross-env NODE_ENV=production SERVER_ENV=production nuxt start",
    "generate": "nuxt generate",
    "lint:js": "eslint --ext \".js,.vue\" --ignore-path .gitignore .",
    "lint": "yarn lint:js"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.12.5",
    "core-js": "^3.8.3",
    "cross-env": "^7.0.3",
    "element-ui": "^2.15.0",
    "nuxt": "^2.14.12",
    "qs": "^6.9.6"
  },
  "devDependencies": {
    "@nuxtjs/eslint-config": "^5.0.0",
    "@nuxtjs/eslint-module": "^3.0.2",
    "babel-eslint": "^10.1.0",
    "prettier": "^2.2.1"
  }
}
